
	<view wx:for="{{problemsData}}" wx:key="{{item.id}}" class="item">
		<view class="hd">
			<!-- <text>{{item.title}}</text> -->
			<text class="title" style="color: #333">{{item.question}}</text>
		</view>
		<view class="bd">
			<text class="info">{{item.answer}}</text>
		</view>
	</view>
